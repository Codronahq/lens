[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "codrona-lens"
version = "0.1.0"
description = "Codrona Lens - data spine: ingestion, warehouse, analytics"
requires-python = ">=3.11"
license = "AGPL-3.0-or-later"
dependencies = [
  "requests>=2.32",
]

[project.optional-dependencies]
spark = [
  "pyspark==4.0.1",
  "pyarrow>=17",
  "duckdb==1.5.5",
]
dev = [
  "pre-commit",
  "ruff",
  "mypy",
  "pytest",
  "pyyaml",
  "types-requests",
  "types-PyYAML",
  "pyspark==4.0.1",
  "dbt-core==1.12.2",
  "dbt-duckdb==1.11.0",
  "pyarrow>=17",
  "duckdb==1.5.5",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
# SIM108 forces a 100-char ternary that reads worse than the if/else it replaces.
ignore = ["SIM108"]

[tool.ruff.lint.per-file-ignores]
# Databricks injects `spark` into the notebook session; it is not importable, so
# every reference reads as undefined to a static checker. Scoped to F821 alone -
# the notebook is otherwise held to the same rules as the rest of the tree.
"notebooks/*.ipynb" = ["F821"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_return_any = true

[[tool.mypy.overrides]]
module = ["repo_checks"]
disallow_untyped_defs = false
disallow_incomplete_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
